# Role "Prajurit" — Implementasi Plan

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Menambahkan role `prajurit` ke Study Center Nias — role ini mirip `student` (bisa mengisi jurnal), dengan tambahan 3 item jurnal khusus, dan panel admin "Jurnal Prajurit" yang memungkinkan admin scan QR prajurit lalu mengisi jurnal hari itu via popup/modal.

**Architecture:**
- Role baru `prajurit` di tabel `roles` (seeder/migration)
- Jurnal prajurit memakai infrastruktur `JurnalEntry` + `JurnalLifeCheck` yang sudah ada
- Admin panel mengikuti pola `ScholarshipTeenagerJurnalAdminController` (trait `HasJurnalAdminActions`)
- Scan QR mengikuti pola `presensi/form.blade.php` (html5-qrcode@2.3.8)
- 3 item jurnal khusus prajurit: **Tidak Memaki**, **Membaca Alkitab di Sekolah**, **Jumlah Salah Ayat Hafalan**

**Tech Stack:** Laravel 11, Blade + AdminLTE 3, Alpine.js, html5-qrcode CDN, existing JurnalEntry/JurnalLifeCheck models

---

## Task 1: Migration — Seeder Role "Prajurit"

**Objective:** Tambah role `prajurit` ke tabel `roles` via migration baru.

**Files:**
- Create: `database/migrations/2026_09_06_000001_add_prajurit_role.php`

**Step 1: Buat migration file**

```php
<?php
use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration {
    public function up(): void {
        DB::table('roles')->insertOrIgnore([
            'name'        => 'prajurit',
            'description' => 'Prajurit — dapat mengisi jurnal melalui scan QR',
            'created_at'  => now(),
            'updated_at'  => now(),
        ]);
    }

    public function down(): void {
        DB::table('roles')->where('name', 'prajurit')->delete();
    }
};
```

**Step 2: Jalankan migration**

```bash
cd /var/www/study-center-nias && php artisan migrate
```

Expected: migration berhasil, role `prajurit` masuk ke DB.

**Step 3: Commit**

```bash
git add database/migrations/2026_09_06_000001_add_prajurit_role.php
git commit -m "feat: add prajurit role migration"
```

---

## Task 2: Migration — 3 Item Jurnal Khusus Prajurit

**Objective:** Tambah 3 `JurnalLifeItem` default baru khusus prajurit ke DB.

**Files:**
- Create: `database/migrations/2026_09_06_000002_seed_prajurit_jurnal_life_items.php`

**Step 1: Buat migration file**

```php
<?php
use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

return new class extends Migration {
    public function up(): void {
        $now = now();
        $items = [
            [
                'kategori'      => 'prajurit',
                'reset_period'  => 'daily',
                'response_type' => 'boolean',
                'label'         => 'Tidak Memaki',
                'is_default'    => true,
                'student_id'    => null,
                'is_active'     => true,
                'created_by'    => null,
                'created_at'    => $now,
                'updated_at'    => $now,
            ],
            [
                'kategori'      => 'prajurit',
                'reset_period'  => 'daily',
                'response_type' => 'boolean',
                'label'         => 'Membaca Alkitab di Sekolah',
                'is_default'    => true,
                'student_id'    => null,
                'is_active'     => true,
                'created_by'    => null,
                'created_at'    => $now,
                'updated_at'    => $now,
            ],
            [
                'kategori'      => 'prajurit',
                'reset_period'  => 'daily',
                'response_type' => 'number',   // jumlah salah = angka
                'label'         => 'Jumlah Salah Ayat Hafalan',
                'is_default'    => true,
                'student_id'    => null,
                'is_active'     => true,
                'created_by'    => null,
                'created_at'    => $now,
                'updated_at'    => $now,
            ],
        ];
        DB::table('jurnal_life_items')->insertOrIgnore($items);
    }

    public function down(): void {
        DB::table('jurnal_life_items')
            ->where('kategori', 'prajurit')
            ->whereNull('student_id')
            ->delete();
    }
};
```

**Step 2: Jalankan**

```bash
php artisan migrate
```

**Step 3: Commit**

```bash
git add database/migrations/2026_09_06_000002_seed_prajurit_jurnal_life_items.php
git commit -m "feat: seed prajurit jurnal life items (tidak memaki, alkitab, ayat hafalan)"
```

> **Catatan:** `JurnalLifeItem` memiliki kolom `response_type` (lihat model). Item "Jumlah Salah Ayat Hafalan" memakai `response_type = 'number'` — di modal admin nanti ditampilkan sebagai `<input type="number" min="0">` bukan checkbox.

---

## Task 3: Route Jurnal Prajurit (Self-Service)

**Objective:** Buat route agar prajurit bisa mengisi jurnal sendiri (mirip `scholarship_teenager`).

**Files:**
- Create: `app/Http/Controllers/Web/Prajurit/PrajuritJurnalController.php`
- Modify: `routes/web.php` — tambah route group baru
- Create: `resources/views/prajurit/jurnal.blade.php`

**Step 1: Buat controller**

```php
<?php
namespace App\Http\Controllers\Web\Prajurit;

use App\Http\Controllers\Controller;
use App\Http\Controllers\Web\Traits\HasJurnalDailyActions;

class PrajuritJurnalController extends Controller
{
    use HasJurnalDailyActions;

    protected string $role        = 'prajurit';
    protected string $routePrefix = 'prajurit-jurnal';
    protected string $viewName    = 'prajurit.jurnal';
    protected array  $kategori    = ['prajurit'];
    protected bool   $showVerse   = false;
}
```

File: `app/Http/Controllers/Web/Prajurit/PrajuritJurnalController.php`

**Step 2: Tambah route ke `routes/web.php`**

Cari bagian scholarship_teenager lalu tambah di bawahnya:

```php
use App\Http\Controllers\Web\Prajurit\PrajuritJurnalController;

Route::middleware(['auth', 'role:prajurit'])->prefix('jurnal-prajurit')->name('prajurit-jurnal.')->group(function () {
    Route::get('/',        [PrajuritJurnalController::class, 'index'])->name('index');
    Route::post('/toggle', [PrajuritJurnalController::class, 'toggle'])->name('toggle');
});
```

**Step 3: Buat view**

File: `resources/views/prajurit/jurnal.blade.php`

Copy `resources/views/scholarship-teenager/jurnal.blade.php` lalu:
- Ganti judul dengan "Jurnal Prajurit"
- Ganti semua route prefix `scholarship-teenager-jurnal` → `prajurit-jurnal`
- Hapus bagian foto, verse, dan items yang tidak relevan (kategori hanya `prajurit`)

**Step 4: Commit**

```bash
git add app/Http/Controllers/Web/Prajurit/ resources/views/prajurit/ routes/web.php
git commit -m "feat: prajurit self-service jurnal route and view"
```

---

## Task 4: Admin Controller — Jurnal Prajurit

**Objective:** Buat controller admin untuk melihat progress prajurit dan trigger isi jurnal via QR scan.

**Files:**
- Create: `app/Http/Controllers/Web/Admin/PrajuritJurnalAdminController.php`

**Step 1: Buat controller**

Ikuti pola `ScholarshipTeenagerJurnalAdminController`. Controller ini perlu method tambahan: `scanJurnal` (terima user_id dari QR, return data prajurit) dan `saveJurnal` (simpan centangan dari modal).

```php
<?php
namespace App\Http\Controllers\Web\Admin;

use App\Http\Controllers\Controller;
use App\Http\Controllers\Web\Admin\Traits\HasJurnalAdminActions;
use App\Models\CollegeBibleItem;
use App\Models\CollegeConfig;
use App\Models\JurnalEntry;
use App\Models\JurnalLifeCheck;
use App\Models\JurnalLifeItem;
use App\Models\Role;
use App\Models\User;
use App\Support\JurnalWeek;
use Illuminate\Http\Request;

class PrajuritJurnalAdminController extends Controller
{
    use HasJurnalAdminActions;

    protected string $role            = 'prajurit';
    protected string $viewPrefix      = 'admin.prajurit-jurnal';
    protected string $csvPrefix       = 'jurnal-prajurit';
    protected string $userVar         = 'targetUser';
    protected string $profileRelation = 'studentProfile';
    protected array  $kategori        = ['prajurit'];

    public function dashboard(Request $request)
    {
        $config       = CollegeConfig::current();
        $dayNo        = $config->todayDayNo();
        $bible        = CollegeBibleItem::forDayNo($dayNo);
        $today        = JurnalWeek::today()->toDateString();
        $sevenDaysAgo = JurnalWeek::today()->subDays(6)->toDateString();
        $roleId       = Role::where('name', 'prajurit')->value('id');

        $usersQ = User::where('is_active', true)
            ->whereHas('roles', fn($r) => $r->where('roles.id', $roleId))
            ->with('studentProfile')
            ->orderBy('name');

        if ($request->filled('q')) {
            $term = '%' . $request->q . '%';
            $usersQ->where(fn($w) => $w
                ->where('name', 'like', $term)
                ->orWhere('username', 'like', $term));
        }

        $users = $usersQ->paginate(30)->withQueryString();

        $activeToday = User::where('is_active', true)
            ->whereHas('roles', fn($r) => $r->where('roles.id', $roleId))
            ->where(fn($q) => $q
                ->whereHas('jurnalLifeChecks', fn($c) =>
                    $c->whereDate('tanggal', $today)->where('checked', true))
            )->count();

        $totalUsers = User::where('is_active', true)
            ->whereHas('roles', fn($r) => $r->where('roles.id', $roleId))
            ->count();

        $userIds = $users->pluck('id');

        $checkCounts = JurnalLifeCheck::whereIn('student_id', $userIds)
            ->whereBetween('tanggal', [$sevenDaysAgo, $today])
            ->where('checked', true)
            ->selectRaw('student_id, COUNT(*) as total')
            ->groupBy('student_id')
            ->pluck('total', 'student_id');

        $lastEntryDates = JurnalEntry::whereIn('student_id', $userIds)
            ->selectRaw('student_id, MAX(tanggal) as last_date')
            ->groupBy('student_id')
            ->pluck('last_date', 'student_id');

        return view('admin.prajurit-jurnal.dashboard', compact(
            'config', 'dayNo', 'bible', 'today',
            'users', 'activeToday', 'totalUsers',
            'checkCounts', 'lastEntryDates'
        ));
    }

    /**
     * AJAX: Terima user_id dari QR scan, validasi prajurit, return data untuk modal jurnal.
     */
    public function scanJurnal(Request $request)
    {
        $request->validate(['user_id' => 'required|integer']);
        $roleId = Role::where('name', 'prajurit')->value('id');

        $prajurit = User::with('studentProfile')
            ->where('id', $request->user_id)
            ->where('is_active', true)
            ->whereHas('roles', fn($r) => $r->where('roles.id', $roleId))
            ->first();

        if (!$prajurit) {
            return response()->json(['status' => 'not_found']);
        }

        $today = JurnalWeek::today()->toDateString();

        // Ambil semua life items kategori prajurit
        $items = JurnalLifeItem::where('kategori', 'prajurit')
            ->where('is_active', true)
            ->orderBy('id')
            ->get();

        // Ambil centangan hari ini untuk prajurit ini
        $checkedIds = JurnalLifeCheck::where('student_id', $prajurit->id)
            ->whereDate('tanggal', $today)
            ->where('checked', true)
            ->pluck('life_item_id')
            ->all();

        // Untuk item number (Jumlah Salah Ayat Hafalan), ambil nilai jika ada
        // Simpan di kolom `value` di jurnal_life_checks — jika belum ada kolom ini,
        // kita pakai pendekatan berbeda (lihat Task 5).
        $numberValues = JurnalLifeCheck::where('student_id', $prajurit->id)
            ->whereDate('tanggal', $today)
            ->whereIn('life_item_id', $items->where('response_type', 'number')->pluck('id'))
            ->get()
            ->pluck('value', 'life_item_id'); // kolom 'value' ditambah di Task 5

        return response()->json([
            'status'   => 'found',
            'prajurit' => [
                'id'    => $prajurit->id,
                'name'  => $prajurit->name,
                'kelas' => $prajurit->studentProfile?->grade_class,
            ],
            'today'      => $today,
            'items'      => $items->map(fn($i) => [
                'id'            => $i->id,
                'label'         => $i->label,
                'response_type' => $i->response_type,
            ]),
            'checkedIds'   => $checkedIds,
            'numberValues' => $numberValues,
        ]);
    }

    /**
     * AJAX: Simpan centangan dari modal jurnal prajurit.
     * Auto-save (tidak perlu submit terpisah).
     */
    public function saveJurnal(Request $request)
    {
        $request->validate([
            'user_id'           => 'required|integer|exists:users,id',
            'tanggal'           => 'required|date',
            'checks'            => 'nullable|array',
            'checks.*.item_id'  => 'required|integer',
            'checks.*.checked'  => 'nullable|boolean',
            'checks.*.value'    => 'nullable|numeric|min:0',
        ]);

        $roleId = Role::where('name', 'prajurit')->value('id');
        $prajurit = User::where('id', $request->user_id)
            ->where('is_active', true)
            ->whereHas('roles', fn($r) => $r->where('roles.id', $roleId))
            ->firstOrFail();

        $tanggal = $request->tanggal;
        $checks  = $request->checks ?? [];

        foreach ($checks as $check) {
            $itemId  = $check['item_id'];
            $checked = (bool) ($check['checked'] ?? false);
            $value   = $check['value'] ?? null;

            JurnalLifeCheck::updateOrCreate(
                [
                    'student_id'   => $prajurit->id,
                    'life_item_id' => $itemId,
                    'tanggal'      => $tanggal,
                ],
                [
                    'checked' => $checked,
                    'value'   => $value,  // kolom tambahan (Task 5)
                ]
            );
        }

        // Buat/update JurnalEntry untuk tanggal ini (tanda sudah ada aktivitas)
        JurnalEntry::updateOrCreate(
            ['student_id' => $prajurit->id, 'tanggal' => $tanggal],
            ['cabang_id'  => $prajurit->cabang_id]
        );

        return response()->json(['status' => 'saved']);
    }
}
```

**Step 2: Commit**

```bash
git add app/Http/Controllers/Web/Admin/PrajuritJurnalAdminController.php
git commit -m "feat: PrajuritJurnalAdminController with dashboard, scanJurnal, saveJurnal"
```

---

## Task 5: Migration — Kolom `value` di `jurnal_life_checks`

**Objective:** Tambah kolom `value` (nullable integer) di tabel `jurnal_life_checks` untuk menyimpan nilai numerik item seperti "Jumlah Salah Ayat Hafalan".

**Files:**
- Create: `database/migrations/2026_09_06_000003_add_value_to_jurnal_life_checks.php`
- Modify: `app/Models/JurnalLifeCheck.php` — tambah `value` ke fillable/casts

**Step 1: Migration**

```php
<?php
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    public function up(): void {
        Schema::table('jurnal_life_checks', function (Blueprint $table) {
            $table->unsignedSmallInteger('value')->nullable()->after('checked')
                ->comment('Nilai numerik untuk item response_type=number (e.g. jumlah salah ayat)');
        });
    }

    public function down(): void {
        Schema::table('jurnal_life_checks', function (Blueprint $table) {
            $table->dropColumn('value');
        });
    }
};
```

**Step 2: Update Model `JurnalLifeCheck`**

Di `app/Models/JurnalLifeCheck.php`, tambah `value` ke `$guarded = []` (sudah guarded-all, aman) dan tambah ke casts:

```php
protected $casts = [
    'tanggal' => 'date',
    'checked' => 'boolean',
    'value'   => 'integer',   // ← tambah baris ini
];
```

**Step 3: Jalankan**

```bash
php artisan migrate
```

**Step 4: Commit**

```bash
git add database/migrations/2026_09_06_000003_add_value_to_jurnal_life_checks.php \
        app/Models/JurnalLifeCheck.php
git commit -m "feat: add value column to jurnal_life_checks for numeric items"
```

---

## Task 6: Routes Admin — Jurnal Prajurit

**Objective:** Daftarkan semua route admin prajurit di `routes/web.php`.

**Files:**
- Modify: `routes/web.php`

**Step 1: Tambah use statement di atas**

```php
use App\Http\Controllers\Web\Admin\PrajuritJurnalAdminController;
```

**Step 2: Tambah route group (setelah group scholarship-teenager admin)**

```php
Route::middleware(['auth', 'role:admin,mentor'])->prefix('admin/jurnal-prajurit')->name('admin.jurnal-prajurit.')->group(function () {
    Route::get('/',                    [PrajuritJurnalAdminController::class, 'dashboard'])->name('index');
    Route::get('/laporan',             [PrajuritJurnalAdminController::class, 'index'])->name('laporan');
    Route::get('/laporan/{user}',      [PrajuritJurnalAdminController::class, 'show'])->name('show');
    Route::get('/laporan/{user}/export', [PrajuritJurnalAdminController::class, 'export'])->name('export');

    // Scan QR → return data prajurit (AJAX)
    Route::post('/scan',               [PrajuritJurnalAdminController::class, 'scanJurnal'])->name('scan');

    // Save jurnal dari modal (AJAX, auto-save)
    Route::post('/save',               [PrajuritJurnalAdminController::class, 'saveJurnal'])->name('save');
});
```

**Step 3: Commit**

```bash
git add routes/web.php
git commit -m "feat: admin prajurit jurnal routes"
```

---

## Task 7: Views Admin — Dashboard + Scan QR + Modal Jurnal

**Objective:** Buat view admin untuk Jurnal Prajurit: halaman daftar prajurit dengan tombol "Scan QR", kamera scanner, dan modal isi jurnal.

**Files:**
- Create: `resources/views/admin/prajurit-jurnal/dashboard.blade.php`
- Create: `resources/views/admin/prajurit-jurnal/index.blade.php` (laporan)
- Create: `resources/views/admin/prajurit-jurnal/show.blade.php`

**Step 1: Buat direktori**

```bash
mkdir -p resources/views/admin/prajurit-jurnal
```

**Step 2: Buat `dashboard.blade.php`**

Copy dari `resources/views/admin/scholarship-teenager-jurnal/dashboard.blade.php`, lalu:

1. Ubah `@section('page-title', 'Jurnal Prajurit')`
2. Ubah semua route `admin.jurnal-scholarship-teenager.*` → `admin.jurnal-prajurit.*`
3. Ubah label "Pengguna Remaja Beasiswa" → "Prajurit"
4. **Tambah tombol "Scan QR" di header card tabel** (sebelah kanan form filter):

```html
<button type="button" class="btn btn-sm btn-warning ml-2" id="btnOpenScanner">
    <i class="fas fa-qrcode mr-1"></i> Scan QR Prajurit
</button>
```

5. **Tambah Modal Scanner + Modal Jurnal di bawah `@endsection` konten:**

```html
{{-- ═══════ MODAL: QR SCANNER ═══════ --}}
<div class="modal fade" id="scannerModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-warning">
                <h5 class="modal-title">
                    <i class="fas fa-qrcode mr-1"></i> Scan QR Prajurit
                </h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span>&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div id="qr-reader" style="width:100%"></div>
                <div id="scan-status" class="mt-2 text-center text-muted small"></div>
            </div>
        </div>
    </div>
</div>

{{-- ═══════ MODAL: JURNAL PRAJURIT ═══════ --}}
<div class="modal fade" id="jurnalModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title" id="jurnalModalTitle">
                    <i class="fas fa-book-open mr-1"></i> Jurnal Prajurit
                </h5>
                <button type="button" class="close text-white" data-dismiss="modal">
                    <span>&times;</span>
                </button>
            </div>
            <div class="modal-body" id="jurnalModalBody">
                <div class="text-muted small mb-3" id="jurnalPrajuritInfo"></div>
                <div id="jurnalItemsList"></div>
                <div id="jurnalSaveStatus" class="mt-2 text-center"></div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-success" id="btnSaveJurnal">
                    <i class="fas fa-save mr-1"></i> Simpan & Tutup
                </button>
            </div>
        </div>
    </div>
</div>
```

**Step 3: Tambah JavaScript di bawah modal (gunakan `@push('scripts')` atau inline)**

Logika JS yang perlu dibuat:

```javascript
// html5-qrcode CDN
// https://unpkg.com/html5-qrcode@2.3.8/html5-qrcode.min.js

const SCAN_URL  = "{{ route('admin.jurnal-prajurit.scan') }}";
const SAVE_URL  = "{{ route('admin.jurnal-prajurit.save') }}";
const CSRF      = "{{ csrf_token() }}";

let scanner     = null;
let scanning    = false;
let currentUser = null;
let currentItems = [];

// ── Tombol buka scanner ──
document.getElementById('btnOpenScanner').addEventListener('click', () => {
    $('#scannerModal').modal('show');
});

// ── Start kamera saat modal scanner tampil ──
$('#scannerModal').on('shown.bs.modal', function () {
    scanner = new Html5Qrcode('qr-reader');
    scanner.start(
        { facingMode: 'environment' },
        { fps: 10, qrbox: { width: 250, height: 250 } },
        onScanSuccess,
        () => {}
    );
});

// ── Stop kamera saat modal scanner ditutup ──
$('#scannerModal').on('hide.bs.modal', function () {
    if (scanner) scanner.stop().catch(() => {}).finally(() => { scanner = null; });
});

// ── Handle hasil scan ──
function onScanSuccess(decodedText) {
    if (scanning) return;
    scanning = true;

    const userId = parseInt(decodedText.trim(), 10);
    if (isNaN(userId)) {
        document.getElementById('scan-status').innerHTML =
            '<span class="text-danger">QR tidak valid</span>';
        setTimeout(() => { scanning = false; }, 2000);
        return;
    }

    document.getElementById('scan-status').innerHTML =
        '<span class="text-info"><i class="fas fa-spinner fa-spin mr-1"></i>Memuat data...</span>';

    fetch(SCAN_URL, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'X-CSRF-TOKEN': CSRF,
            'Accept': 'application/json',
        },
        body: JSON.stringify({ user_id: userId }),
    })
    .then(r => r.json())
    .then(data => {
        if (data.status === 'not_found') {
            document.getElementById('scan-status').innerHTML =
                '<span class="text-danger">Prajurit tidak ditemukan atau tidak aktif.</span>';
            setTimeout(() => { scanning = false; }, 2500);
            return;
        }

        // Valid → tutup scanner, buka modal jurnal
        currentUser  = data.prajurit;
        currentItems = data.items;

        $('#scannerModal').modal('hide');
        openJurnalModal(data);
        scanning = false;
    })
    .catch(() => {
        document.getElementById('scan-status').innerHTML =
            '<span class="text-danger">Koneksi gagal.</span>';
        setTimeout(() => { scanning = false; }, 2000);
    });
}

// ── Buka modal jurnal dan isi form ──
function openJurnalModal(data) {
    document.getElementById('jurnalModalTitle').innerHTML =
        `<i class="fas fa-book-open mr-1"></i> Jurnal — ${escHtml(data.prajurit.name)}`;
    document.getElementById('jurnalPrajuritInfo').innerHTML =
        `Kelas: <strong>${escHtml(data.prajurit.kelas || '—')}</strong> &nbsp;·&nbsp; Tanggal: <strong>${data.today}</strong>`;

    // Build form items
    let html = '';
    data.items.forEach(item => {
        const checked = data.checkedIds.includes(item.id);
        if (item.response_type === 'boolean') {
            html += `
            <div class="custom-control custom-checkbox mb-2">
                <input type="checkbox" class="custom-control-input jurnal-check"
                    id="item_${item.id}" data-item-id="${item.id}" data-type="boolean"
                    ${checked ? 'checked' : ''}>
                <label class="custom-control-label" for="item_${item.id}">
                    ${escHtml(item.label)}
                </label>
            </div>`;
        } else {
            // response_type = 'number'
            const val = data.numberValues[item.id] ?? '';
            html += `
            <div class="form-group mb-2">
                <label for="item_num_${item.id}">${escHtml(item.label)}</label>
                <input type="number" min="0" class="form-control jurnal-number"
                    id="item_num_${item.id}" data-item-id="${item.id}" data-type="number"
                    value="${val}" placeholder="0">
            </div>`;
        }
    });
    document.getElementById('jurnalItemsList').innerHTML = html;
    document.getElementById('jurnalSaveStatus').innerHTML = '';
    $('#jurnalModal').modal('show');
}

// ── Simpan jurnal (auto-save saat klik Simpan & Tutup) ──
document.getElementById('btnSaveJurnal').addEventListener('click', saveJurnal);

function saveJurnal() {
    const checks = [];

    document.querySelectorAll('.jurnal-check').forEach(el => {
        checks.push({
            item_id: parseInt(el.dataset.itemId),
            checked: el.checked,
            value: null,
        });
    });

    document.querySelectorAll('.jurnal-number').forEach(el => {
        checks.push({
            item_id: parseInt(el.dataset.itemId),
            checked: (el.value > 0),  // centang otomatis jika ada nilai
            value: parseInt(el.value) || 0,
        });
    });

    document.getElementById('jurnalSaveStatus').innerHTML =
        '<span class="text-info"><i class="fas fa-spinner fa-spin mr-1"></i>Menyimpan...</span>';

    fetch(SAVE_URL, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'X-CSRF-TOKEN': CSRF,
            'Accept': 'application/json',
        },
        body: JSON.stringify({
            user_id: currentUser.id,
            tanggal: document.getElementById('jurnalPrajuritInfo')
                .textContent.match(/\d{4}-\d{2}-\d{2}/)?.[0],
            checks: checks,
        }),
    })
    .then(r => r.json())
    .then(data => {
        if (data.status === 'saved') {
            $('#jurnalModal').modal('hide');
            // Refresh halaman ringan untuk update tabel
            location.reload();
        }
    })
    .catch(() => {
        document.getElementById('jurnalSaveStatus').innerHTML =
            '<span class="text-danger">Gagal menyimpan. Coba lagi.</span>';
    });
}

function escHtml(str) {
    return String(str).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
}
```

**Step 4: Tambah CDN html5-qrcode di view (via `@push('scripts')` atau setelah `@stack('scripts')` di layout)**

```html
<script src="https://unpkg.com/html5-qrcode@2.3.8/html5-qrcode.min.js"></script>
```

**Step 5: Buat view laporan dan show**

Copy dari:
- `resources/views/admin/scholarship-teenager-jurnal/index.blade.php` → `admin/prajurit-jurnal/index.blade.php`
- `resources/views/admin/scholarship-teenager-jurnal/show.blade.php` → `admin/prajurit-jurnal/show.blade.php`

Ganti semua route prefix dan label yang relevan.

**Step 6: Commit**

```bash
git add resources/views/admin/prajurit-jurnal/
git commit -m "feat: admin prajurit jurnal views — dashboard dengan QR scan + modal jurnal"
```

---

## Task 8: Sidebar Admin — Tambah Menu "Jurnal Prajurit"

**Objective:** Tambah menu navigasi di sidebar admin.

**Files:**
- Modify: `resources/views/layouts/admin.blade.php`

**Step 1: Temukan bagian Jurnal Remaja, tambah setelah treeview-nya:**

```html
<li class="nav-item has-treeview {{ request()->routeIs('admin.jurnal-prajurit.*') ? 'menu-open' : '' }}">
    <a href="#" class="nav-link {{ request()->routeIs('admin.jurnal-prajurit.*') ? 'active' : '' }}">
        <i class="nav-icon fas fa-shield-alt"></i>
        <p>Jurnal Prajurit<i class="right fas fa-angle-left"></i></p>
    </a>
    <ul class="nav nav-treeview">
        <li class="nav-item">
            <a href="{{ route('admin.jurnal-prajurit.index') }}"
               class="nav-link {{ request()->routeIs('admin.jurnal-prajurit.index') ? 'active' : '' }}">
                <i class="far fa-circle nav-icon"></i><p>Progress</p>
            </a>
        </li>
        <li class="nav-item">
            <a href="{{ route('admin.jurnal-prajurit.laporan') }}"
               class="nav-link {{ request()->routeIs('admin.jurnal-prajurit.laporan') || request()->routeIs('admin.jurnal-prajurit.show') ? 'active' : '' }}">
                <i class="far fa-circle nav-icon"></i><p>Laporan</p>
            </a>
        </li>
    </ul>
</li>
```

**Step 2: Commit**

```bash
git add resources/views/layouts/admin.blade.php
git commit -m "feat: add Jurnal Prajurit to admin sidebar"
```

---

## Task 9: User Management — Tambah Role Prajurit ke Dropdown

**Objective:** Pastikan admin bisa assign role `prajurit` saat create/edit user.

**Files:**
- Inspect: `resources/views/admin/users_edit.blade.php` dan `users_create.blade.php`

**Step 1: Cek apakah role di-hardcode atau dinamis**

```bash
grep -n "prajurit\|student\|college\|role" resources/views/admin/users_edit.blade.php | head -20
```

Jika dropdown role mengambil dari DB (`Role::all()`), maka otomatis muncul setelah migration Task 1 dijalankan — tidak perlu ubah apapun.

Jika hardcoded, tambah option `prajurit` ke daftar.

**Step 2: Verifikasi di AdminController**

```bash
grep -n "roles\|Role::" app/Http/Controllers/Web/Admin/AdminController.php | head -10
```

Pastikan `createUser` dan `editUser` pass `$roles = Role::all()` ke view.

**Step 3: Commit (jika ada perubahan)**

```bash
git add resources/views/admin/users_create.blade.php resources/views/admin/users_edit.blade.php
git commit -m "fix: ensure prajurit role appears in user create/edit dropdown"
```

---

## Task 10: Navigasi Prajurit — Menu Setelah Login

**Objective:** Pastikan user dengan role `prajurit` memiliki link ke jurnal mereka setelah login.

**Files:**
- Inspect: `resources/views/layouts/` — temukan navbar/menu untuk authenticated users
- Modify: tambah link ke `prajurit-jurnal.index` jika user `hasRole('prajurit')`

**Step 1: Cek layout yang dipakai student/scholarship_teenager**

```bash
grep -rn "prajurit-jurnal\|scholarship-teenager-jurnal\|college-jurnal" resources/views/layouts/ | head -10
```

**Step 2: Tambah conditional di layout:**

```blade
@if(auth()->user()->hasRole('prajurit'))
    <a href="{{ route('prajurit-jurnal.index') }}" class="nav-link">
        <i class="fas fa-shield-alt mr-1"></i> Jurnal Saya
    </a>
@endif
```

**Step 3: Commit**

```bash
git add resources/views/layouts/
git commit -m "feat: add prajurit jurnal nav link for prajurit role users"
```

---

## Task 11: Test Manual & Smoke Check

**Objective:** Verifikasi end-to-end bahwa fitur bekerja.

**Checklist:**

1. **DB Setup**
   - [ ] Role `prajurit` ada di tabel `roles`
   - [ ] 3 item jurnal dengan `kategori = 'prajurit'` ada di `jurnal_life_items`
   - [ ] Kolom `value` ada di `jurnal_life_checks`

2. **Admin Panel**
   - [ ] Sidebar menampilkan menu "Jurnal Prajurit"
   - [ ] `/admin/jurnal-prajurit` menampilkan daftar prajurit (kosong awalnya)
   - [ ] Tombol "Scan QR" membuka modal dengan kamera

3. **QR Scan Flow**
   - [ ] Scan QR user prajurit → modal jurnal muncul dengan nama & kelas
   - [ ] Scan QR user NON-prajurit → pesan "tidak ditemukan"
   - [ ] Centang "Tidak Memaki", "Membaca Alkitab di Sekolah" → checkbox bekerja
   - [ ] Input angka "Jumlah Salah Ayat Hafalan" → number input bekerja
   - [ ] Klik "Simpan & Tutup" → modal tertutup, halaman refresh, user tampil di tabel
   - [ ] Scan ulang QR user yang sama hari itu → centangan sebelumnya terisi

4. **Jurnal Mandiri (Prajurit)**
   - [ ] User prajurit login → ada link ke `/jurnal-prajurit`
   - [ ] Halaman jurnal tampil dengan 3 item prajurit

5. **Edge Cases**
   - [ ] QR tidak valid (bukan angka) → error message
   - [ ] User prajurit tidak aktif → `not_found`

**Perintah verifikasi cepat:**

```bash
# Cek route terdaftar
php artisan route:list | grep prajurit

# Cek migrasi sudah jalan
php artisan migrate:status | tail -5
```

---

## Risiko & Catatan

| Risiko | Mitigasi |
|--------|----------|
| `response_type` kolom di `jurnal_life_items` mungkin tidak ada | Cek migrasi awal: `grep response_type database/migrations/` — jika tidak ada, cukup pakai `label` saja untuk membedakan behavior |
| Kolom `value` di `jurnal_life_checks` belum ada | Task 5 menambahkannya; jika ada constraint PK composite, pastikan `updateOrCreate` keys lengkap |
| `JurnalLifeCheck` primary key adalah composite (lihat model: `$incrementing = false`) | `updateOrCreate` butuh semua kolom composite sebagai key: `student_id`, `life_item_id`, `tanggal` |
| Kamera tidak muncul di non-HTTPS | Pastikan dev environment pakai HTTPS atau localhost |
| `HasJurnalAdminActions` trait punya method `index()` — pastikan tidak konflik dengan dashboard | Method `dashboard()` ada di controller, `index()` (laporan) dari trait — tidak konflik |
| Modal scan + modal jurnal bertumpuk | `$('#scannerModal').modal('hide')` sebelum `$('#jurnalModal').modal('show')` — ada delay kecil karena animasi Bootstrap; tambah timeout 400ms jika diperlukan |

---

## File Summary

| File | Status |
|------|--------|
| `database/migrations/2026_09_06_000001_add_prajurit_role.php` | **Baru** |
| `database/migrations/2026_09_06_000002_seed_prajurit_jurnal_life_items.php` | **Baru** |
| `database/migrations/2026_09_06_000003_add_value_to_jurnal_life_checks.php` | **Baru** |
| `app/Models/JurnalLifeCheck.php` | **Modifikasi** (tambah `value` ke casts) |
| `app/Http/Controllers/Web/Prajurit/PrajuritJurnalController.php` | **Baru** |
| `app/Http/Controllers/Web/Admin/PrajuritJurnalAdminController.php` | **Baru** |
| `routes/web.php` | **Modifikasi** (2 route group baru) |
| `resources/views/prajurit/jurnal.blade.php` | **Baru** |
| `resources/views/admin/prajurit-jurnal/dashboard.blade.php` | **Baru** |
| `resources/views/admin/prajurit-jurnal/index.blade.php` | **Baru** |
| `resources/views/admin/prajurit-jurnal/show.blade.php` | **Baru** |
| `resources/views/layouts/admin.blade.php` | **Modifikasi** (sidebar menu) |
| `resources/views/layouts/*.blade.php` | **Modifikasi** (nav link prajurit) |
